# 1.2.0：粒子与拖尾导入

这是插件源码更新，适用于经典版本 800 MDX，不是仅为某个 Blender 场景手工添加特效。

## 安装及使用

1. 在偏好设置的插件列表禁用旧版，从磁盘安装 `io_scene_warcraft_3_1.2.0.zip`；安装后重启 Blender，并勾选启用 **WarCraft 3 .mdx importer**，检查版本为 1.2.0。
2. 在新场景重新导入 MDX。保留 **Import Particles and Ribbons** 勾选。
3. 默认每秒采样 20 次，每个发射器最多 256 个粒子。可提高 **Effects Samples / Second** 让特效更平滑，代价是导入时间和 blend 文件大小增加。超过粒子上限时显示提示。
4. 选中模型的 Nodes 骨架，在数据属性 / WarCraft 3 的动画列表中切换动作；视图使用材质预览或渲染显示。

旧场景不会自动增加特效。请通过插件动画列表切换，单独在动作编辑器切换骨架 Action 不会同步网格颜色和特效 Action。

## 支持的内容

- PRE2：发射位置、父节点、速度、变化量、发射角、重力、寿命、速率、发射区域、生命周期颜色/透明度/大小、Head/Tail/Both、图集 UV、可见性轨道。
- 粒子 ModelSpace、LineEmitter、XYQuad 标志；普通头部粒子采用交叉面片近似，不依赖场景相机。
- Squirt 突发发射及特效参数的全局循环；特效参数轨道支持阶跃、线性、Hermite、Bezier。
- RIBB：根据运动轨迹生成带状几何，读取上下高度、寿命、发射速率、重力、颜色、透明度、可见性和纹理槽位；停止发射后已有尾迹按寿命消退。
- 透明、加色、AlphaKey 特效材质；直接编辑 FX 材质中的图像纹理节点即可替换图片。
- 同目录查找对应 PNG/TGA/DDS，以及插件配置的资源目录。BLP 仍需事先转换。
- 缺失特效图片时生成并内嵌柔边预览图，发射器参数保留，导入报告和骨架面板提示原始文件名。
- 特效位于独立的“模型名 Effects”集合，不要把身体材质批量关联给这个集合。

## 动画存储方式

特效在导入时按动作烘焙为形态键，并由原生几何节点读取颜色、UV、透明度。每个模型保存自己的动作绑定，允许改名和重复导入。播放时没有 Python 帧回调，不创建或删除物体，不依赖临时磁盘缓存。

特效会随整个骨架对象移动、缩放，但轨迹是导入时烘焙的。之后修改骨骼动作不会自动重新模拟特效，需要重新导入/烘焙。随机结果固定；拖回同一帧得到相同结果。时间采样使用阶跃，避免粒子出生/死亡时在不同位置间错误插值。

## 当前限制

- 普通粒子采用交叉面片近似游戏里的朝相机面片；不是游戏渲染器逐像素复刻。
- 缺少原图时的柔边预览不等同于原始烟雾图集/刀光图片；补齐图片后重新导入可获得更接近的形状。
- Modulate/Modulate2x 使用透明混合近似。深度排序、优先层、游戏雾效和引擎特有混合规则没有逐项复现。
- PREM 的外部模型粒子、事件触发的外部模型/声音、Reforged 高版本 MDX 未新增支持；非空 PREM 会明确提示。
- 原有普通模型的多层材质、纹理动画及骨骼全局序列处理仍存在旧实现限制；本版没有宣称完整实现全部 MDX 功能。特效节点跟随当前导入骨架的动作求值。
- 自动图像查找只匹配模型引用的名字。例如 `Nefarian.blp` 对应 `Nefarian.png`，不会自动猜测 `Nefarian2.png`。

## 本机验证

Blender 5.2.2 LTS：

- 原骨骼动画回归测试：同名动作隔离、节点 ID、转义名称、保存重开等。
- 9 项独立轨道测试：插值、全局循环、序列隔离和无效全局引用。
- 合成 MDX 完整导入：PRE2/RIBB、粒子头尾、几何属性、关闭特效、全局突发发射、错误记录拒绝。
- 用户 Nefarian.mdx：3 个粒子发射器、1 条拖尾、11 个动作；验证静止/攻击/施法可见性、倒放定位、动作切换和保存重开；播放前后物体数量不变。
- 真实模型 Cycles 与 Eevee 渲染检查，均确认粒子可见。加色材质使用 Blended 模式，避免 Eevee 的 Dithered 模式隐藏粒子。当前文件夹缺少 `Textures\Clouds8x8.blp`、`Textures\RibbonBlur1.blp` 的对应图片，因此使用柔边预览。

二进制字段核对参考：

- https://github.com/flowtsohg/mdx-m3-viewer/blob/master/src/parsers/mdlx/particleemitter2.ts
- https://github.com/flowtsohg/mdx-m3-viewer/blob/master/src/parsers/mdlx/ribbonemitter.ts
- https://docs.blender.org/manual/en/5.2/render/eevee/limitations/nodes_support.html

实现未引入外部依赖，用户的 MDX/贴图不打包进插件。

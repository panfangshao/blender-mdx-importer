# 1.2.1 更新缓存修复

已定位真实运行窗口中的问题：插件名称页显示 1.2.0，但 `importer` 和 `operators` 子模块仍是 1.1.2 的内存版本，操作器甚至没有 `importEffects` 属性。于是虽然重新导入了带 PRE2/RIBB 的 MDX，旧代码仍跳过特效。

Blender 从 ZIP 更新插件时可能只重新载入 `__init__.py`。本次为插件增加按依赖顺序重载子模块的逻辑，并在仍有注册类时先注销旧类，避免版本显示与实际执行代码不一致。

安装 `io_scene_warcraft_3_1.2.1.zip` 后启用。导入窗口应出现 **Import Particles and Ribbons**，默认勾选。重新导入 Nefarian.mdx 后应生成 **Nefarian Effects** 集合，包含四个 FX 对象。

已验证禁用后更新、启用状态重载及 1.1.2 -> 1.2.1 ZIP 更新路径，确认操作器属性、导入实现和菜单同步更新。

特效支持范围与已知近似见《特效更新说明.md》。
